---
name: ivan-learning
description: 英语听力口语带练 Skill（美漫梗学习法）。用《恶搞之家》(Family Guy) 等美剧/美漫/动画的名场面梗片段，按「盲听→逐句解析→带字幕精听」流程带学习者一步步学。触发场景：用户想学英语、练听力/口语、想用美剧美漫/动画/电影学英语、要求挑片段做听力训练、要求盲听逐句解析精听、英语等级测评（A1-C1）、按等级定制英语学习计划、英语学习打卡监督。开场必做：先询问学习者 CEFR 等级（A1-C1），不知道就出分级测试评估，再按等级挑片段、出课，并可设置定时监督。
license: MIT
---

# 美漫梗英语听力口语带练

用美剧/美漫名场面梗片段，带学员做「盲听 → 逐句解析 → 带字幕精听」训练，可按需加定时监督打卡。

## 0. 等级评估（每次新学员必做，先于一切）

1. 先直接问学员 CEFR 等级（A1-C1 之间自报）。
2. 若不知道或不确定：读 `references/level-test.md`，按其中的分级测试出题评估（15 道 MC，五档各 3 题，连续通过定级）。
3. 定级后记录到长期偏好（manage_preference：等级 + 训练偏好），后续所有片段按此等级适配。

## 0.5 复习上一课（第 2 次及以后调用必做）

每次开新课前，先完成上一课的复习闭环：

1. 先要求学员用上一课的 HTML 互动页做复习：整段盲听重听 → 精听区逐句跟读 → 交出上节课布置的作业（造句/填空/录音）。
2. 批改复习作业（纠音、检查造句），通过后才进入新课；不通过则让学员重练并重交。
3. **顺序（学员确认的节奏）**：先交付上一课复习入口（课程索引页/上一课 HTML 链接）→ 学员复习并反馈/交作业 → 批改通过后，再开始收集新课素材（选片段→核台词→找片源→裁剪→制作新课 HTML）。
4. 交付节奏：同一轮先交付「复习任务 + 上一课反馈」，学员确认复习完成后，再交付新课 HTML 页。

## 1. 选片段：先核台词，再找片源

读 `references/clip-selection.md` 后执行，顺序不可颠倒：

1. 确定候选片段：知名梗/名场面、对话密集、有清晰发音点或句型点。
2. **先网搜该片段完整台词并核验**（如 subslikescript 字幕站），按等级适配维度评估（生词率、语速、连读密度、句式复杂度、梗可解释性）。
3. 等级不适配就换片段，不硬教。
4. 再找视频源：国内用户优先 B 站（site:bilibili.com 搜索，剪辑/合集可下载），YouTube 备选。**先探测本机代理**（有代理 yt-dlp 加 `--proxy` 下全网；无代理则 B 站直连 / YouTube 找 B 站搬运或走公共代理——完整通道见 `references/clip-selection.md` 第 3 节），下载默认多线程。
5. 下载后抽帧 OCR 还原剪辑版音频中的**真实台词**（剪辑常跳句，硬字幕即证据），按台词时间轴用 ffmpeg 裁剪出无字幕场景视频 + 音频（字幕遮挡用 boxblur 模糊、不裁画面，方法见 `references/clip-selection.md` 第 6 节；裁剪起点必须含首句完整开头，用 ASR 验证）。
6. 版权边界：只裁剪 1-2 分钟内短片段用于个人学习；不下载、不传播完整剧集。

## 1.5 工作模式（硬性，每次出课遵守）

1. **资料必须匹配学员等级**：搜索的台词/片段/解析/例句全部按学员当前 CEFR 等级适配。生词率、句式复杂度、梗的可解释性都要落在该等级带内；A1-A2 用短句+高频词，B1 用短句+少量习语，B2-C1 可上复杂从句与生僻梗。搜到素材先对等级表过一遍，不符就换，不用超纲内容凑数。
2. **精讲例句总量控制在 ~20 句**：每课精讲区累计给出的可跟读例句（含每句原句 + 扩展例句 + 例句扩展块 `ex`）合计约 20 句。不要每句都塞满例句（会稀释密度），也不要只有原句没例句（太薄）。
3. **例句要有趣、可复用**：例句贴合生活/梗/学员能直接搬去用的口语场景，能带点趣味或双关更好；避免教科书式死句。例句里的词组/句型必须真实出自本课原句，扩展词显式标注「本课未出现」。
4. **密度优先**：与其堆多句稀汤寡水，不如把 3-5 句核心梗句做厚（缩略/习语/连读 + 例句），保证「学东西学得到」。

## 2. 出课：四步教学法

**产线铁律：页面必须从 `assets/lesson-template.html` 派生，并严格按 `references/html-build.md` 搭建**——模板含官方 CSS/JS（视觉、播放器双轨逻辑、cue 防串句），任何模型不得另起炉灶重写样式与交互，保证所有学员拿到的页面体验一致。素材准备（台词提取、时间轴对齐、字幕遮挡）按 `references/clip-selection.md` 执行。

读 `references/lesson-template.md` 按交付流程执行，四步缺一不可：

1. **盲听**：交付内嵌「无字幕视频+原声」的单文件 HTML（标准见 `references/html-build.md` 第 5-6 节；源带硬字幕先 crop 掉字幕条再内嵌）+ 3-5 个引导问题；学员看画面听 2-3 遍，先不看文字。
2. **逐句解析**：台词表格（编号/说话人/台词/解析），解析覆盖发音连读、词汇、句型、文化梗；解析必须与音频实际内容一致。
3. **带字幕精听**：交付干净双语脚本，按步骤：对照字幕再听一遍 → 标出盲听时没听出的点 → 逐句跟读 2 遍 → 最后裸听检验。
4. **监督（可选）**：问清每天可学时长 + 提醒时间 → 按定时任务 Skill 建每日打卡 → 学员交作业（填空/造句/录音）→ 批改纠音、按表现调整下一课难度。

## 3. 质量门

- 台词不编造：字幕站全文 + 视频硬字幕 OCR + 云端 ASR 三重核验（见 `references/clip-selection.md` 第 4 节）；字幕与官方台词有差异要标注。
- 每课交付：单文件 HTML 互动学习页（内嵌无字幕视频 + 音频，含盲听/精讲/精听），从 `assets/lesson-template.html` 派生，按 `references/html-build.md` 第 7 节三重验证（base64 字节一致、时长一致、Playwright 浏览器实开自检）通过后再交付。
- 逐课难度只升不跳：学员跟读/造句达标后再升等级档。

## 参考文件

- `references/level-test.md`：A1-C1 分级测试（15 题 + 评分规则 + 口语/写作复核）。
- `references/clip-selection.md`：**台词提取 · 音频对齐 · 字幕遮挡 · 片源准备**完整操作手册（三重校准、时间轴、crop 字幕、ffmpeg 参数）。
- `references/html-build.md`：**课程 HTML 页面搭建规范**（结构、视觉规范、播放器双轨逻辑、base64 内嵌、生成后验证、常见问题）。
- `references/lesson-template.md`：四步教学交付流程 + 复习闭环 + 生产规避清单。
- `assets/lesson-template.html`：**官方课程页模板**（完整 CSS/JS + 数据占位），出课必须从它派生。
